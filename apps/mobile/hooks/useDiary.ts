import {} from "./useDatabase"

export const useDiary = () => {
	//TODO implement this hook for fetching diary entries
	// all was done in the old useDiary, that now is useDatabase
	// so something that needs caching/automatic refresh can be done here

	return {}
}
